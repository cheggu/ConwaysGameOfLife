#pragma once
#include <SFML/Graphics.hpp>
#include <vector>
#include <string>
#include <iostream>
#include <fstream>
#include <set>
#include <iterator>

#define WIDTH 800
#define HEIGHT 600

sf::RenderWindow window(sf::VideoMode(WIDTH, HEIGHT), "Cheggu's Game of Life");
